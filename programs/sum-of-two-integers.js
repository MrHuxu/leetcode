var getSum = function(a, b) {
  var base = 1, result = 0, tmpa, tmpb, tmp = 0;
  var orTmp, andTmp;
  while (a || b) {
    tmpa = a & base;
    tmpb = b & base;

    orTmp = tmpa | tmpb;
    andTmp = tema & tmpb;
    if (tmpa | tmpb) {
      if (orTmp && andTmp && tmp) {
        
      }
    }
  }
};