/**
 * @param {number} N
 * @return {boolean}
 */
const { expect } = require('chai');

let divisorGame = function(N) {
    
};

describe('', () => {
  it('divisor-game', () => {
    expect(divisorGame(2)).to.eq(true);
    expect(divisorGame(3)).to.eq(false);
  });
});