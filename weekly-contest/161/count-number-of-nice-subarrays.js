const { expect } = require('chai');

/**
 * @param {number[]} nums
 * @param {number} k
 * @return {number}
 */
let numberOfSubarrays = function(nums, k) {
  

  return result;
};

it('count-number-of-nice-subarrays', () => {
  expect(numberOfSubarrays([1,1,2,1,1], 3)).to.eq(2);
  expect(numberOfSubarrays([2,4,6], 1)).to.eq(0);
  expect(numberOfSubarrays([2,2,2,1,2,2,1,2,2,2], 2)).to.eq(16);
});